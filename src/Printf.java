public class Printf {

    public static void main(String[] args){
        //printf - is a method to format and display text
        // this makes adding variables to print classes easier

        boolean Boolean = true;
        char Char = '@';
        String string = "meow";
        int Int = 100;
        double Double = 3.14;

        System.out.printf("this is a format %c",Char);
        /*
        %b = boolean
        %c = char
        %s = String
        %d = int and any non decimal data types
        %f = for doubles and floats
         */
        /* add a number between the percent sign and the letter to
        specify how many spaces will it have between the variable and the text */

        // precision units = used for doubles and floats to limit number of decimals displayed by adding ".[any number here]"

    }
}
